# Design QA — 图谱产品完整性验收

验收日期：2026-07-24

验收范围：接待详情关系社群图、聚合状态转移图、目标标签对比/溯源/洞察。

验收视口：Codex in-app browser，1487 × 1058。

## 1. 验收目标与证据规则

本轮验证三类图谱是否已经合理进入 AudioGraphy 原有产品导航，并在浅色视觉体系下形成可用的“单接待详情 → 跨接待聚合 → 标签洞察”分析闭环。

三张用户参考图与三张当前实现截图均为 1487 × 1058。每个对照文件都按原始尺寸依次放入同一张图，参考图在上、当前实现在下；没有重绘、裁剪、拉伸或单独调整比例。因此可直接比较首屏信息密度、侧栏占比、拓扑层级和视觉语言。

截图和同图对照保存在本地 `.design-audit/product-completeness/`，该目录含用户提供的临时素材，因此按仓库策略不提交 Git；本报告只记录可复现的验收方法与文件名。

## 2. 分页验收结论

### 2.1 接待详情 / 关系社群图

健康度：良好，可进入产品验收。

- 当前截图状态：接待 `#101`，`关系与溯源` 页签和 `关系图谱` 模式已选中；右侧节点详情面板处于“等待选择节点”的初始状态。
- 参考匹配：保留了浅色大画布、核心接待、外围语义社群、同心轮廓、社群内连线、跨社群虚线和高价值线索提示。
- 产品融合：图谱位于原平台 `接待中心 → 接待详情 → 关系与溯源`，没有另起一套孤立导航；调听与切分仍是相邻主任务。
- 交互状态：图谱模式可切换；节点支持鼠标与键盘激活；选择节点后更新右侧详情；缩放、重置和全屏控制具有可读名称。
- 可接受差异：实现侧保留原平台全局菜单、详情上下文页签和节点检查器，因此画布可用宽度小于参考图；当前数据按录音、对话单元、状态和目标标签组织，不复制参考图中的客户画像文案。

对照证据：

- 参考图：用户提供的接待关系社群图
- 当前实现：`after/03-reception-graph.png`
- 同图对照：`comparisons/01-reception-detail-reference-vs-implementation.png`

### 2.2 跨接待聚合 / 状态转移图

健康度：良好，可进入产品验收。

- 当前截图状态：`状态路径` 页面，`全部路径` 已选中；六个阶段完整进入首屏，路径证据面板在该截图中未展开。
- 参考匹配：保留了六阶段胶囊、连续主路径、回流路径、异常语义、左侧聚合筛选、顶部关键指标和画布缩放控制。
- 产品融合：页面位于原平台 `对话洞察 → 状态路径`，可返回接待中心，并可从聚合路径继续下钻单接待详情。
- 交互状态：关键路径、全部路径、异常跳转可切换；阶段节点和路径均可通过鼠标或键盘打开证据；时间范围会在请求前校验；无数据、接口失败和数据截断均有显式状态。
- 可接受差异：实现将参考图中的话术、动作和标签明确标为“场景参考/建议/示例”，避免把模板误解为真实统计；为了让六阶段在原平台侧栏下完整可见，阶段卡片更紧凑，证据抽屉按需打开而非默认常驻。

对照证据：

- 参考图：用户提供的聚合状态转移图
- 当前实现：`after/04-state-flow.png`
- 同图对照：`comparisons/02-state-flow-reference-vs-implementation.png`

### 2.3 目标标签对比 / 溯源 / 洞察

健康度：良好，可进入产品验收。

- 当前截图状态：`标签洞察 → 关系图谱` 已选中，右侧已打开 `stage.requirement` 的标签对比与溯源面板，并显示冲突、版本、置信度、来源和证据时间码。
- 参考匹配：保留了目标标签中心、浅色语义社区、关系线、冲突强调、右侧版本对照与证据下钻；平台顶部同时保留跨门店、销售、场景、接待、标签组和时间筛选。
- 产品融合：页面位于原平台 `对话洞察 → 标签洞察`；关系图谱、对比矩阵、图表分析是同一分析任务的内部视图，不挤占全局菜单。
- 交互状态：视图按钮具有真实选中状态；社区、节点类型和“只看冲突”可筛选；节点支持鼠标与键盘激活；缩放与重置可用；关系图、矩阵、图表之间的页面内切换不会破坏 HashRouter 路由。
- 可接受差异：参考图使用更自由的径向分布，实现采用确定性的多社区网格，优先保证多标签组、多版本和截断数据下节点不重叠、边不悬空；当前截图展示的是关系图视图，矩阵和趋势图表位于同页其他内部视图。

对照证据：

- 参考图：用户提供的目标标签对比与溯源图
- 当前实现：`after/05-tag-insights.png`
- 同图对照：`comparisons/03-tag-insights-reference-vs-implementation.png`

## 3. 跨页面交互与状态完整性

- 信息架构：三页已进入同一平台框架；单接待分析放在接待详情上下文，聚合状态与标签洞察放在跨接待分析上下文。
- 选择反馈：导航使用当前页状态，图谱模式、路径模式、洞察视图和冲突筛选使用真实 pressed 状态，而不是仅依赖颜色。
- 证据下钻：详情节点、聚合阶段/路径、标签节点和矩阵行均具有对应证据入口；缺少时间码时不会生成伪跳转。
- 数据边界：聚合数据、标签矩阵和图谱节点达到接口上限时会显示截断语义；空状态和错误状态不会留下无解释的空白画布。
- 数据输入：聚合开始/结束时间会校验先后关系；门店和销售筛选数量有边界，避免生成无界查询。

## 4. 可访问性检查

已由组件实现和针对性测试确认：

- 上下文导航使用具名 `nav` 与当前页状态，不再模拟不完整的 tab 语义。
- 图谱节点、状态阶段和路径可获取焦点，并支持 Enter/Space 激活。
- 模式、筛选和节点选择使用 `aria-pressed` 暴露状态；缩放、重置、全屏、关闭面板等图标按钮均有可读名称。
- 图谱、图例、筛选面板、证据面板和数据表具有区域或标题关联。
- 时间范围错误使用 `aria-invalid` 和 `aria-describedby` 关联错误说明。
- 标签洞察的页面内滚动遵循 `prefers-reduced-motion`。

仍需人工可访问性验收：

- 截图不能证明完整 WCAG 符合性；仍需使用真实屏幕阅读器确认复杂 SVG 的阅读顺序和关系表达。
- 需要在纯键盘下复核图谱缩放、全屏、节点遍历和抽屉关闭后的焦点回归。
- 当前视觉验收仅覆盖 1487 × 1058 桌面视口；200% 浏览器缩放、窄屏重排和高对比模式尚未形成截图证据。
- 高密度节点文字在 100% 画布下偏小，虽然可通过缩放查看，仍应在真实长标签与系统字体替换场景下继续观察可读性。

## 5. 已知数据与环境限制

- 当前截图来自 Sites 本地演示模式，并显式显示 `交互演示 · 模拟数据`。这些数据用于验证页面结构和核心交互，不等于生产数据库验收。
- 真实后端要显示完整图谱，必须存在同租户的用户、录音、接待、对话单元、状态转移、标签版本、溯源关系和证据时间码；空数据库会按设计进入空状态。
- 调听跳转只有在真实媒体地址和有效时间码存在时才能完成端到端播放验证；截图仅能证明入口与证据展示，不能证明音频传输质量。
- 聚合接口和标签矩阵采用有界返回，超出范围的历史数据不会假装为全量；UI 会提示可见范围或截断状态。
- 本轮没有用移动端或不同浏览器重复截图，也没有以演示数据替代真实后端的数据完整性结论。

## 6. 结论

在 1487 × 1058 的目标桌面视口下，三类图谱均已达到“浅色、分层、可溯源、与原平台融合”的核心设计目标，未观察到阻断验收的布局裁切、不可见主操作或错误页面归属。视觉构图不逐字复制参考数据，但保留了参考图最关键的层级、社区、路径、冲突和证据语言。

结论：**桌面视觉与核心交互通过，生产数据、真实音频、响应式和辅助技术仍需独立环境验收。**
