"""Leaf observability package — metric definitions every layer may import.

Depends on nothing inside ``audio_graphy``, so core / storage / services can
record metrics without pulling in the API layer.
"""

from __future__ import annotations
